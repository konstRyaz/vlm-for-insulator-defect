# Конфигурации

- `pipeline/` — конфигурации Stage 3/Stage 4 и detector-to-VLM запусков.
- `pipeline/prompts/` — prompt-файлы для VLM.
- `stage4_detector_to_vlm_pred_val_context_pad030.yaml` — ключевая настройка context crop с padding `0.30`.
